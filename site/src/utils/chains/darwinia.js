import { capitalizeFirstLetter } from "..";

const value = "darwinia";

export const darwinia = {
  value,
  name: capitalizeFirstLetter(value),
  symbol: "RING",
  decimals: 18,
  hasSubscan: true,
};
