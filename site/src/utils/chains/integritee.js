import { capitalizeFirstLetter } from "..";

const value = "integritee";

export const integritee = {
  value,
  name: capitalizeFirstLetter(value),
  symbol: "TEER",
  decimals: 12,
  hasSubscan: true,
};
