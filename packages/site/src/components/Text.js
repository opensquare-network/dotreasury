import styled from "styled-components";

import { TEXT_DARK_MAJOR } from "../constants";

const Text = styled.p`
  font-family: "Inter";
  font-style: normal;
  font-weight: normal;
  font-size: 14px;
  line-height: 24px;
  color: ${TEXT_DARK_MAJOR};
  margin: 0;
`;

export default Text;
