{{#*inline "commentUrl"}}{{siteUrl}}/{{pluralize indexer.type}}/{{indexer.index}}?page={{page}}#{{commentId}}{{/inline}}

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style type="text/css">
    body,
    p,
    div,
    pre {
      font-family: 'Inter', 'Lato', 'Helvetica Neue', Arial, Helvetica, sans-serif;
      font-family: Inter;
      font-style: normal;
      font-weight: normal;
      font-size: 14px;
      line-height: 24px;
    }

    a {
      color: #4183c4;
      text-decoration: none;
    }

    a:hover {
      color: #40a9ff;
      text-decoration: underline;
    }

    pre {
      background-color: whitesmoke;
      color: #4a4a4a;
      overflow-x: auto;
      padding: 1rem 1rem;
      white-space: pre;
      word-wrap: normal;
    }

    p {
      margin: 0;
      padding: 0;
    }

    .container {
      margin: 0 auto;
      max-width: 600px;
    }
  </style>
</head>

<body>
  <div class="container">
    <p>
      Hi {{mentioned}}!<br /><br />

      <br />
      {{author}} has mentioned you in comment: <a href="{{> commentUrl}}">{{> commentUrl}}</a>.<br /><br />

      comment: <pre>{{{content}}}</pre><br /><br />

      You can deactivate this notification in your notification settings: <a
        href="{{siteUrl}}/settings/notification">{{siteUrl}}/settings/notification</a><br /><br />

      Dotreasury Team
    </p>
  </div>
</body>

</html>
