{{#*inline "commentUrl"}}{{siteUrl}}/{{#if (isdefined indexer.tipId)}}tips/{{indexer.tipId.blockHeight}}_{{indexer.tipId.tipHash}}{{/if}}{{#if (isdefined indexer.proposalId)}}proposals/{{indexer.proposalId}}{{/if}}{{#if (isdefined indexer.bountyId)}}bounties/{{indexer.bountyId}}{{/if}}?page={{page}}#{{commentId}}{{/inline}}

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style type="text/css">
    </style>
  </head>
  <body>
    <div>
      <p>
        Hi {{mentioned}}!<br/><br/>

        <br />
        {{author}} has mentioned you in comment: <a href="{{> commentUrl}}">{{> commentUrl}}</a>.<br /><br />

        comment: {{{content}}}<br /><br />

        Dotreasury Team
      </p>
    </div>
  </body>
</html>
